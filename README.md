# Selenium Chrome with Auth-Proxy Setup

This repository provides a setup guide and sample code to run Selenium tests in Chrome while using an authentication proxy.

## Requirements

- Selenium
- ChromeDriver
- Python
